{
  "name": "ap",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rsbuild build",
    "build:wasm": "./build.sh",
    "dev": "rsbuild dev --open",
    "preview": "rsbuild preview"
  },
  "devDependencies": {
    "@rsbuild/core": "^1.2.4",
    "rsbuild-plugin-wasmpack": "^0.0.3",
    "typescript": "^5.7.3"
  }
}
