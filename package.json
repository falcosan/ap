{
  "name": "ap",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rsbuild build",
    "build:wasm": "./build.sh",
    "preview": "rsbuild preview",
    "dev": "npm run build:wasm && rsbuild dev --open"
  },
  "devDependencies": {
    "@rsbuild/core": "^1.2.4",
    "typescript": "^5.7.3"
  }
}
